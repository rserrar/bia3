Mode: EVOLUTION

Objectiu: millorar el parent model amb canvis petits o mitjans, hipòtesi clara i memòria explícita del que ja s'ha provat a la família.
EVOLUTION ha de prioritzar explotació de línies prometedores i millora competitiva incremental.

### Context
num_new_models={{num_new_models}}

parent_model:
{{parent_model_json}}

family_models:
{{family_models_json}}

family_metrics_summary:
{{family_metrics_summary}}

improvement_focus:
{{improvement_focus}}

genealogy_case_studies:
{{genealogy_case_studies}}

conversation_history_summary:
{{conversation_history_summary}}

available_inputs_description:
{{available_inputs_description}}

available_inputs_contract_json:
{{available_inputs_contract_json}}

available_outputs_description:
{{available_outputs_description}}

available_outputs_contract_json:
{{available_outputs_contract_json}}

architecture_guide_content:
{{architecture_guide_content}}

### Regles principals
1. El parent model és la base: no redisseny total.
2. Canvis incrementals, justificats i fàcils d'atribuir; màxim 1 canvi estructural menor i 1-2 refinaments.
3. Potencia la varietat: no et limitis a canvis topologics. Considera tambe:
   - Canviar quines entrades s'usen (afegir/treure features com volum si estan a available_inputs_description)
   - Modificar sortides auxiliars
   - Variar la profunditat/complexitat de les capes (mes/menys capes, diferents units)
   - Reorganitzar branques o fluxos interns
4. Evita repetir patrons de la família sense variació real, especialment si el context mostra saturació sense breakthrough.
5. Explica clarament què canvies respecte al parent, quin mecanisme concret hauria d'ajudar a millorar SL/TB i/o `decision_score` versus referències fortes de família, i quines serien les següents variants.
6. Prioritza estabilitat build/compile/smoke-fit.
7. Si dubtes d'schema o consistència, simplifica; si dubtes de competitivitat, escull el refinament amb més potencial explicable.
8. Si una proposta implica salt arquitectònic fort, NO és evolution: reconduir-la a mutació incremental.
9. No inventis capes, merge types ni noms d'inputs/outputs que no existeixin al contracte; si tens dubtes, usa només els valors literals de `available_inputs_contract_json` i `available_outputs_contract_json`.
10. Usa `conversation_history_summary` per evitar repetir mutacions, recomanacions o hipòtesis de família que ja s'han provat sense resultat.

### Política de sortides
- `stop_loss_prediction` i `take_profit_prediction` són obligatories a tots els candidats.
- Recomanació general: si afegeixes auxiliars, prova com a màxim una família auxiliar principal per candidat: o bé `min_7_days_prediction`/`max_7_days_prediction`, o bé `reconstruct_prices_last_100_target`.
- Barrejar alhora `min/max 7d`, reconstrucció i moltes altres auxiliars s'ha provat poques vegades i, en els casos vistos fins ara, no ha donat bons resultats; tendeix a diluir el focus sobre `SL/TB`.
- Si no tens una hipòtesi clara sobre l'ajuda de les auxiliars, prefereix `SL/TP` sols o `SL/TP + 1` auxiliar principal.

### Capes suportades i paràmetres mínims
El model pot usar les següents capes suportades pel runtime de la V3. Cada capa ha de respectar els paràmetres mínims indicats.
- `Dense`: requereix `units`.
- `Dropout`: usa `rate`.
- `SpatialDropout1D`: usa `rate` i és especialment útil en branques temporals/convolucionals.
- `Conv1D`: requereix `kernel_size` i `filters`.
- `SeparableConv1D`: requereix `kernel_size`, `filters`, i `depth_multiplier` si s'especifica.
- `LSTM` / `GRU`: requereixen `units`.
- `Bidirectional`: requereix `wrapped_layer` o `inner_layer` amb `LSTM` o `GRU`.
- `TimeDistributed`: requereix `wrapped_layer` o `inner_layer`.
- `AttentionKeras` / `MultiHeadAttentionKeras`: requereixen tensors de rank >=3.
- `Add` / `Multiply` / `Concatenate` dins `branches[].layers[]` o `merges[].layers_after_merge[]`: requereixen `input_source_feature_maps` amb >=2 tensors.
- `AttentionKeras`: dins capes, requereix `input_source_feature_maps` amb 2-3 tensors 3D.
- `MultiHeadAttentionKeras`: dins capes, requereix `input_source_feature_maps` amb 1-3 tensors 3D.
- Si uses `MultiHeadAttentionKeras`, defineix sempre `input_source_feature_maps` de manera explícita; si no tens clar query/value/key, no usis atenció i substitueix-la per `concatenate + Dense`.
- No facis `AttentionKeras` o `MultiHeadAttentionKeras` directament sobre seqüències crues de longitud `800`; primer redueix longitud amb pooling/downsampling o treballa sobre una representació comprimida.
- `merges[]`: requereixen `source_feature_maps` amb >=2 tensors.
- `MaxPooling1D` / `AveragePooling1D` / `GlobalMaxPooling1D` / `GlobalAveragePooling1D` / `Flatten`.
- `RepeatVector`, `GaussianNoise`, `Masking`, `Activation`, `Softmax`, `ReLU`, `LeakyReLU`, `PReLU`, `ELU`.
- `BatchNormalization`, `LayerNormalization`, `Reshape`, `LambdaSlice`.
- `Reshape`: usa `target_shape` per adaptar tensors 2D/3D.
- `MaxPooling1D` / `AveragePooling1D`: si s'especifiquen, usa `pool_size`.
- `Activation`: si s'usa com a capa explícita, defineix `activation_function`.
- `MultiHeadAttentionKeras`: defineix `constructor_params.num_heads` i `constructor_params.key_dim`.
- Tipus NO permesos: qualsevol capa o merge type que no estigui a la llista suportada. Exemples incorrectes: `Subtract`, `ModulatedFusion`, `CrossAttention`, `ResidualBlock`.
- Consulta `architecture_guide_content` per exemples funcionals amb CNN, RNN, attention, fusió multi-branca i regularització.

### Regles de format i consistència
- No utilitzis claus legacy dins `architecture_definition`: `inputs`, `input_layers`, `shared_backbone`.
- Si `num_new_models=1`, retorna un objecte JSON amb `model_definition_full` i `model_definition_summary`.
- Si `num_new_models>1`, retorna `{ "candidates": [ ... ] }` amb exactament `num_new_models` elements.
- No emboliquis la resposta dins de markdown ni blocs de codi.
- Utilitza noms d'inputs i targets exclusivament dels `available_inputs_description` i `available_outputs_description`.
- Per `used_inputs`, copia literalment `input_layer_name`, `source_feature_name` i `shape` des d'`available_inputs_contract_json`.
- Per `output_heads`, copia literalment `maps_to_target_config_name`, `output_layer_name` i `units` des d'`available_outputs_contract_json`.

### Procés mental recomanat
1. Llegeix primer el `parent_model`, `family_models`, `family_metrics_summary` i `genealogy_case_studies` abans de dissenyar res.
1b. Llegeix `conversation_history_summary` i evita repetir patrons que la família ja hagi descartat o esgotat.
2. Formula una hipòtesi principal breu i atribuïble: quin canvi hauria de millorar SL/TB o decision_score.
3. Decideix explícitament què mantens del parent perquè ja funciona, i què canvies perquè sospites que és el coll d'ampolla.
4. Dissenya la mutació més petita possible que permeti validar aquesta hipòtesi.
5. Construeix el wiring i les shapes capa per capa; no assumeixis que una idea arquitectònica és vàlida només perquè sona raonable.
6. Abans de retornar, revisa si el JSON final ajuda una futura generació a entendre què s'ha provat i com continuar l'experiment.

### Regles de wiring intern (CRITIQUES)
- Nomes pots referenciar `feature_maps` que ja existeixin explicitament en aquell punt del graf.
- Fonts valides per referencies internes: `used_inputs[].input_layer_name`, `branches[].output_feature_map_name`, `merges[].output_feature_map_name`, o `layers[].name` / `layers_after_merge[].name` ja definits abans.
- No inventis noms intermedis de gates o projections (`gw_*`, `gate_weights_*`, etc.) si no hi ha una capa real anterior amb aquest `name`.
- `input_source_layer` d'una branca ha de ser un sol nom de `feature_map` existent; no pot ser `None`, buit, ni una llista.
- Si vols combinar diverses fonts, fes-ho amb branques separades + `merges[]`, no passant una llista directament com a input de branca.
- Cada merge ha de tenir sempre `output_feature_map_name` no buit i unic.
- Si una branca o merge crea un tensor que despres es reutilitza, posa-li `output_feature_map_name` o `name` explicit i reutilitza exactament aquell nom.

### Camps obligatoris de context experimental
Has de retornar aquests camps dins `model_definition_full`:
- `model_id`
- `version`
- `description`
- `change_log`
- `llm_reasoning`
- `potential_next_variations`
- `seed`
- `architecture_definition`
- `training_config`

### Camps permesos dins `training_config`
- `batch_size` (int): mostres per gradient step
- `epochs` (int): nombre maxim d'epochs
- `validation_split` (float): fraccio per validacio
- `early_stopping_patience` (int): epochs sense millora per parar
- `reduce_lr_patience` (int): epochs sense millora per reduir lr
- `reduce_lr_factor` (float): factor de reduccio de lr
- `min_lr` (float): lr minim
- `restore_best_weights` (bool): restaurar millors pesos
- `initial_learning_rate` (float): lr inicial
- `optimizer` (string): nom de l'optimitzador
- `seed` (int): llavor aleatoria
- `target_metric` (string): metrica per early stopping
- `target_metric_mode` (string): min o max
- `max_training_minutes` (int): temps maxim d'entrenament
- `soft_max_epochs` (int): cap maxima d'epochs (soft)
- `business_metric_sl_weight` (float): pes SL a la metrica de negoci
- `business_metric_tb_weight` (float): pes TB a la metrica de negoci
- `business_min_relative_improvement` (float): millora minima relativa
- `business_improvement_window` (int): finestra per estancament

### Contingut obligatori dels camps de text
- `description`: resum curt de la mutació respecte al parent.
- `change_log`: llista de canvis específics respecte al parent o millor fill de referència.
- `llm_reasoning`: quina hipòtesi vols comprovar, què esperes millorar, mecanisme competitiu esperat (especialment SL/TB i `decision_score`) i per què la mutació és coherent amb la història de la família.
- `potential_next_variations`: 2-4 següents mutacions lògiques si aquest experiment funciona o falla parcialment.

### Memòria per a futures generacions (MOLT IMPORTANT)
El que escrius a `change_log`, `llm_reasoning`, `potential_next_variations` i `model_definition_summary` servirà de guia per a futures mutacions de la família.
Per tant:
- explica el canvi de manera concreta i reutilitzable, no genèrica
- deixa clar quin senyal vols aprofitar (`SL`, `TB`, recència, rang, volum, etc.)
- deixa clar quina continuació seria lògica si el resultat és bo
- deixa clar quina simplificació o correcció provaries si el resultat és dolent
- evita frases vagues com "millora general" o "més capacitat" sense mecanisme causal

### model_definition_summary obligatori
`model_definition_summary` ha de ser un objecte curt però útil amb aquests camps obligatoris:
- `kind`
- `focus`
- `inputs`
- `outputs`
- `complexity`
- `expected_behavior`
- `change_strategy`
- `what_changed`
- `experiment_goal`
- `next_variations`
- `competitiveness_hypothesis`

### Instrucció especial d'evolució
Fes servir el `parent_model`, els `family_models`, `family_metrics_summary` i `genealogy_case_studies` per evitar repeticions.
Has d'explicar implícitament en `change_log`, `llm_reasoning` i `model_definition_summary.what_changed`:
- què canvies del parent,
- què no canvies perquè ja funciona,
- què vols validar amb aquesta mutació,
- quines serien les següents variants si els resultats són bons o dolents.

Si `family_models` és buit o molt petit, tracta-ho com a família poc explorada:
- aprofita especialment el `parent_model` i `genealogy_case_studies`
- proposa una mutació conservadora però informativa
- fes que `potential_next_variations` sigui especialment útil per construir memòria experimental a partir d'aquest primer fill

### Guardrails d'arquitectura
- `architecture_definition` obligatori.
- `used_inputs` obligatori i no buit.
- `branches` obligatori i no buit.
- `output_heads` obligatori i no buit.
- `merges` opcional, però si existeix ha de ser llista.
- Cada item de `merges` ha de tenir `name`, `type`, `source_feature_maps` (llista no buida) i `output_feature_map_name`.
- No inventis inputs/targets fora dels disponibles.
- `used_inputs[]` han de coincidir exactament amb alguna entrada d'`available_inputs_contract_json`.
- `output_heads[]` han de coincidir exactament amb alguna entrada d'`available_outputs_contract_json` pel que fa a `maps_to_target_config_name`, `output_layer_name` i `units`.
- Mantén coherència de `maps_to_target_config_name`.
- `Add`/`Multiply`/`Concatenate` dins capes: `input_source_feature_maps` >=2.
- `merges[]`: `source_feature_maps` >=2.
- `merges[].type` ha de ser només `concatenate`, `add` o `multiply`.
- `AttentionKeras`/`MultiHeadAttentionKeras`: rank >=3.
- `AttentionKeras`/`MultiHeadAttentionKeras` dins capes requereixen `input_source_feature_maps` no buit.
- `TimeDistributed`: inner layer suportada.
- `Bidirectional`: requereix `wrapped_layer` o `inner_layer`.
- `Dense` requereix `units`; `Conv1D` requereix `kernel_size`; `GRU/LSTM` requereixen `units`.
- En general, `SL/TP only` o `SL/TP + reconstruction` estan sent més robustos que variants massa carregades o massa exòtiques.
- Si una branca usa `Conv1D`, `LSTM`, `GRU`, `AttentionKeras` o `MultiHeadAttentionKeras`, assegura't que rep un tensor 3D; si cal, afegeix `Reshape` abans.
- No apliquis `AttentionKeras` o `MultiHeadAttentionKeras` sobre la sortida de `Flatten`, `GlobalMaxPooling1D` o `GlobalAveragePooling1D`; fes l'atenció abans del pooling.
- Si uses `Add` o `Multiply`, les entrades han de tenir exactament la mateixa shape; si no, projecta-les abans amb una capa compatible o simplifica la mutació.
- Si uses `Concatenate`, les entrades han de ser compatibles excepte a l'eix de concatenació.
- Si introdueixes residuals o gates, comprova explícitament que el tensor de senyal i el tensor de gate/projecció tenen dimensions compatibles.
- No barregis `input_source_feature_maps` i `source_feature_maps` al mateix objecte.
- Si la mutació introdueix attention o wiring complex, prefereix un patró funcional d'`architecture_guide_content` o del parent abans que un redisseny improvisat.
- Per defecte, prefereix `concatenate` simple; usa `multiply` o `add` nomes si pots verificar clarament tots els tensors i les seves shapes.
- Si uses gating multiplicatiu, evita crear pesos de gate amb noms implicits; defineix una capa real amb `name` i reutilitza nomes aquest `feature_map` existent.
- Si uses `Reshape`, la nova shape ha de ser compatible amb el nombre total d'elements del tensor d'entrada; no improvisis dimensions arbitraries.
- Evita branques `range` ambigües amb inputs compostos; si necessites `min` i `max`, prefereix branques separades i una fusio explicita posterior.

### Claus prohibides (dialecte legacy)
No utilitzis aquestes claus dins `architecture_definition`:
- `inputs`
- `input_layers`
- `shared_backbone`
- `layers` a nivell arrel de `architecture_definition`

### Mini schema obligatori (V3)
`model_definition_full.architecture_definition` ha de tenir aquest patró:
```json
{
  "used_inputs": [
    {
      "input_layer_name": "...",
      "source_feature_name": "...",
      "shape": [800]
    }
  ],
  "branches": [
    {
      "name": "...",
      "input_source_layer": "...",
      "layers": [
        {"type": "Dense", "units": 64, "activation": "relu", "name": "..."}
      ],
      "output_feature_map_name": "..."
    }
  ],
  "merges": [],
  "output_heads": [
    {
      "output_layer_name": "...",
      "maps_to_target_config_name": "...",
      "source_feature_map": "...",
      "units": 1,
      "activation": "linear"
    }
  ]
}
```

Si utilitzes `merges`, cada merge ha de seguir aquest format mínim:
```json
{
  "name": "fusion_main_aux",
  "type": "concatenate",
  "source_feature_maps": ["main_features", "aux_features"],
  "layers_after_merge": [
    {"type": "Dense", "units": 64, "activation": "relu", "name": "fusion_dense"}
  ],
  "output_feature_map_name": "shared_features"
}
```

### Auto-check abans de retornar
Verifica internament:
- JSON vàlid
- camps obligatoris
- **CRITICAL: el format de sortida ha de coincidir amb num_new_models. Si num_new_models>1, has de retornar {"candidates": [...]} amb exactament num_new_models elements.**
- `description`, `change_log`, `llm_reasoning`, `potential_next_variations` plens
- `model_definition_summary` amb tots els camps obligatoris
- inputs/outputs consistents
- capes suportades
- que el JSON parseja correctament en un parser JSON estàndard
- que no hi ha claus legacy prohibides
- que la mutació és realment incremental (no redisseny fort)
- que cada candidat justifica explícitament com intenta millorar SL/TB i/o `decision_score` en comparació amb referències fortes de família
- que cada `merge` usa `source_feature_maps`
- que cada capa multi-input usa `input_source_feature_maps`
- que no hi ha `MultiHeadAttentionKeras` sense `constructor_params.num_heads` i `key_dim`
- que no hi ha `Add`/`Multiply` amb shapes incompatibles evidents
- que no hi ha capes seqüencials/atenció rebent tensors 2D quan necessiten 3D
- que no hi ha capes o merges inventats fora del catàleg suportat
- que cada `used_input` i cada `output_head` respecten literalment els contracts JSON
- que cada `input_source_layer`, `source_feature_map`, `input_source_feature_maps` i `source_feature_maps` apunta a un nom real ja definit
- que cap branca usa `input_source_layer=None`, buit o una llista
- que cap merge deixa `output_feature_map_name` buit
- que cap `Reshape` canvia el nombre total d'elements de manera incompatible
- que no hi ha referències indexades o sufixades (`*_0`, `*_1`, `*_proj`, `*_weights`) sense definició real prèvia
- que qualsevol `MultiHeadAttentionKeras` té `input_source_feature_maps` no buit i verificable

### Sortida (OBLIGATÒRIA) — LLEGEIX AIXO PRIMER
Return ONLY valid JSON. Do not include any extra text.
Never prefix or suffix with any text, explanation, or code markers.

CRITICAL: num_new_models DETERMINES THE FORMAT.
Do NOT default to a single object. You MUST check num_new_models before deciding the format.

- IF num_new_models = 1 → return a single JSON object with model_definition_full and model_definition_summary.
- IF num_new_models > 1 → return a JSON object with a "candidates" key containing an ARRAY of exactly num_new_models elements, each with model_definition_full and model_definition_summary.

Before returning, validate that the response is valid JSON and parseable.

Exemple per num_new_models=2 (MULTI-CANDIDATE FORMAT — use when num_new_models>1):
```json
{
  "candidates": [
    {
      "model_definition_full": { ... },
      "model_definition_summary": { ... }
    },
    {
      "model_definition_full": { ... },
      "model_definition_summary": { ... }
    }
  ]
}
```

Exemple per num_new_models=1 (SINGLE FORMAT — use when num_new_models=1):
```json
{
  "model_definition_full": {
    "model_id": "llm_generated_placeholder",
    "version": "1.0.0",
    "description": "...",
    "change_log": ["..."],
    "llm_reasoning": "...",
    "potential_next_variations": ["...", "..."],
    "seed": 42,
    "architecture_definition": {},
    "training_config": {
      "batch_size": 1024,
      "epochs": 500,
      "initial_learning_rate": 0.001,
      "early_stopping_patience": 10
    }
  },
  "model_definition_summary": {
    "kind": "...",
    "focus": "...",
    "inputs": 0,
    "outputs": 0,
    "complexity": "low|medium|high",
    "expected_behavior": "...",
    "change_strategy": "...",
    "what_changed": "...",
    "experiment_goal": "...",
    "next_variations": ["...", "..."],
    "competitiveness_hypothesis": "..."
  }
}
```
