Mode: EXPLORATION

Objectiu: proposar una arquitectura nova, robusta i controlada, inspirada en bons models de famílies diferents sense fer-ne una còpia trivial.
La proposta ha de tenir intenció competitiva explícita: orientar-se a millorar especialment SL/TB (i globalment `decision_score`) respecte candidates recents, no només ser vàlida.

Process guidance (curt): analitza patrons útils del context, tria 1 aposta estructural clara + 1 refinament, i mantén la proposta entrenable.
Reusable memory guidance (curt): reutilitza idees/mecanismes que hagin funcionat en històric (fusió, capes, flux temporal), adaptant-los al nou cas en lloc de copiar literalment.

### Context
num_new_models={{num_new_models}}

best_models_global:
{{best_models_global_json}}

metrics_summary:
{{metrics_summary}}

exploration_hints:
{{exploration_hints}}

genealogy_case_studies:
{{genealogy_case_studies}}

available_inputs_description:
{{available_inputs_description}}

available_outputs_description:
{{available_outputs_description}}

### Regles principals
1. Inspira't en els models bons, però evita còpies trivials.
2. EXPLORATION ha d'incloure obligatòriament un novelty budget: almenys 1 canvi estructural fort + 1 canvi menor de refinament.
3. Canvi estructural fort vol dir canvi de topologia/flux (ex: nova estratègia de fusió, nova organització de branques, nou camí temporal o mecanisme d'atenció). Canvis de units/dropout/lr sols NO compten.
4. Prioritza arquitectures entrenables i estables, però evita propostes només "safe" si no aporten potencial competitiu.
5. Explica clarament què canvies, quin mecanisme concret hauria d'ajudar a millorar SL/TB i/o `decision_score` versus referències fortes, i quines serien les següents variants útils.
6. Si dubtes d'schema o consistència, simplifica; si dubtes de competitivitat, prefereix canvi estructural justificat.
7. Penalitza repetir mateix kind/estratègia de fusió/família de solució quan el context indiqui saturació o manca de breakthroughs.
8. Pots reaprofitar idees de models antics forts, però evita clonar famílies saturades; combina patrons de fonts diferents quan sigui possible.

### Capes suportades i paràmetres mínims
El model pot usar les següents capes suportades pel runtime de la V3. Cada capa ha de respectar els paràmetres mínims indicats.
- `Dense`: requereix `units`.
- `Conv1D`: requereix `kernel_size` i `filters`.
- `SeparableConv1D`: requereix `kernel_size`, `filters`, i `depth_multiplier` si s'especifica.
- `LSTM` / `GRU`: requereixen `units`.
- `Bidirectional`: requereix `wrapped_layer` o `inner_layer` amb `LSTM` o `GRU`.
- `TimeDistributed`: requereix `wrapped_layer` o `inner_layer`.
- `AttentionKeras` / `MultiHeadAttentionKeras`: requereixen tensors de rank >=3.
- `Add` / `Multiply` / `Concatenate`: requereixen `input_source_feature_maps` amb >=2 tensors.
- `MaxPooling1D` / `AveragePooling1D` / `GlobalMaxPooling1D` / `GlobalAveragePooling1D` / `Flatten`.
- `RepeatVector`, `GaussianNoise`, `Masking`, `Activation`, `Softmax`, `ReLU`, `LeakyReLU`, `PReLU`, `ELU`.
- `BatchNormalization`, `LayerNormalization`, `Reshape`, `LambdaSlice`.

### Regles de format i consistència
- No utilitzis claus legacy dins `architecture_definition`: `inputs`, `input_layers`, `shared_backbone`.
- Si `num_new_models=1`, retorna un objecte JSON amb `model_definition_full` i `model_definition_summary`.
- Si `num_new_models>1`, retorna `{ "candidates": [ ... ] }` amb exactament `num_new_models` elements.
- No emboliquis la resposta dins de markdown ni blocs de codi.
- Utilitza noms d'inputs i targets exclusivament dels `available_inputs_description` i `available_outputs_description`.

### Camps obligatoris de context experimental
Has de retornar aquests camps dins `model_definition_full`:
- `model_id`
- `version`
- `description`
- `change_log`
- `llm_reasoning`
- `potential_next_variations`
- `seed`
- `architecture_definition`
- `training_config`

### Camps permesos dins `training_config`
- `batch_size` (int): mostres per gradient step
- `epochs` (int): nombre maxim d'epochs
- `validation_split` (float): fraccio per validacio
- `early_stopping_patience` (int): epochs sense millora per parar
- `reduce_lr_patience` (int): epochs sense millora per reduir lr
- `reduce_lr_factor` (float): factor de reduccio de lr
- `min_lr` (float): lr minim
- `restore_best_weights` (bool): restaurar millors pesos
- `initial_learning_rate` (float): lr inicial
- `optimizer` (string): nom de l'optimitzador
- `seed` (int): llavor aleatoria
- `target_metric` (string): metrica per early stopping
- `target_metric_mode` (string): min o max
- `max_training_minutes` (int): temps maxim d'entrenament
- `soft_max_epochs` (int): cap maxima d'epochs (soft)
- `business_metric_sl_weight` (float): pes SL a la metrica de negoci
- `business_metric_tb_weight` (float): pes TB a la metrica de negoci
- `business_min_relative_improvement` (float): millora minima relativa
- `business_improvement_window` (int): finestra per estancament

### Contingut obligatori dels camps de text
- `description`: resum curt i concret del disseny i la seva intenció.
- `change_log`: llista de canvis concrets respecte als models de referència globals.
- `llm_reasoning`: hipòtesi principal, què esperes millorar o comprovar, mecanisme de millora competitiu (especialment SL/TB i `decision_score`), i per què aquesta proposta és diferent.
- `potential_next_variations`: 2-4 idees concretes per a futures iteracions.

### model_definition_summary obligatori
`model_definition_summary` ha de ser un objecte curt però útil amb aquests camps obligatoris:
- `kind`
- `focus`
- `inputs`
- `outputs`
- `complexity`
- `expected_behavior`
- `change_strategy`
- `what_changed`
- `experiment_goal`
- `next_variations`
- `competitiveness_hypothesis`
- `novelty_budget`

### Guardrails d'arquitectura
- `architecture_definition` obligatori.
- `used_inputs` obligatori i no buit.
- `branches` obligatori i no buit.
- `output_heads` obligatori i no buit.
- `merges` opcional, però si existeix ha de ser llista.
- Cada item de `merges` ha de tenir `name`, `type`, `source_feature_maps` (llista no buida) i `output_feature_map_name`.
- No inventis noms d'inputs fora de `available_inputs_description`.
- No inventis targets fora de `available_outputs_description`.
- `Add`/`Multiply`/`Concatenate` requereixen `input_source_feature_maps` amb >=2 tensors.
- `AttentionKeras`/`MultiHeadAttentionKeras` requereixen tensors de rank >=3.
- `TimeDistributed` només amb inner layer suportada (ex. Dense).
- `Bidirectional` requereix `wrapped_layer` o `inner_layer`.
- `Dense` requereix `units`; `Conv1D` requereix `kernel_size`; `GRU/LSTM` requereixen `units`.

### Claus prohibides (dialecte legacy)
No utilitzis aquestes claus dins `architecture_definition`:
- `inputs`
- `input_layers`
- `shared_backbone`
- `layers` a nivell arrel de `architecture_definition`

### Mini schema obligatori (V3)
`model_definition_full.architecture_definition` ha de tenir aquest patró:
```json
{
  "used_inputs": [
    {
      "input_layer_name": "...",
      "source_feature_name": "...",
      "shape": [800]
    }
  ],
  "branches": [
    {
      "name": "...",
      "input_source_layer": "...",
      "layers": [
        {"type": "Dense", "units": 64, "activation": "relu", "name": "..."}
      ],
      "output_feature_map_name": "..."
    }
  ],
  "merges": [],
  "output_heads": [
    {
      "output_layer_name": "...",
      "maps_to_target_config_name": "...",
      "source_feature_map": "...",
      "units": 1,
      "activation": "linear"
    }
  ]
}
```

Si utilitzes `merges`, cada merge ha de seguir aquest format mínim:
```json
{
  "name": "fusion_main_aux",
  "type": "concatenate",
  "source_feature_maps": ["main_features", "aux_features"],
  "layers_after_merge": [
    {"type": "Dense", "units": 64, "activation": "relu", "name": "fusion_dense"}
  ],
  "output_feature_map_name": "shared_features"
}
```

### Auto-check abans de retornar
Verifica internament:
- JSON vàlid
- camps obligatoris presents
- **CRITICAL: el format de sortida ha de coincidir amb num_new_models. Si num_new_models>1, has de retornar {"candidates": [...]} amb exactament num_new_models elements.**
- `description`, `change_log`, `llm_reasoning`, `potential_next_variations` plens
- `model_definition_summary` amb tots els camps obligatoris
- inputs/outputs consistents
- capes suportades
- que el JSON parseja correctament en un parser JSON estàndard
- que no hi ha claus legacy prohibides
- que cada candidat te 1 canvi estructural fort + 1 refinament menor
- que cada candidat justifica explícitament com intenta millorar SL/TB i/o `decision_score` en comparació amb referències fortes

### Sortida (OBLIGATÒRIA) — LLEGEIX AIXO PRIMER
Return ONLY valid JSON. Do not include any extra text.

CRITICAL: num_new_models DETERMINES THE FORMAT.
Do NOT default to a single object. You MUST check num_new_models before deciding the format.

- IF num_new_models = 1 → return a single JSON object with model_definition_full and model_definition_summary.
- IF num_new_models > 1 → return a JSON object with a "candidates" key containing an ARRAY of exactly num_new_models elements, each with model_definition_full and model_definition_summary.

Before returning, validate that the response is valid JSON and parseable.

Exemple per num_new_models=2 (MULTI-CANDIDATE FORMAT — use when num_new_models>1):
```json
{
  "candidates": [
    {
      "model_definition_full": { ... },
      "model_definition_summary": { ... }
    },
    {
      "model_definition_full": { ... },
      "model_definition_summary": { ... }
    }
  ]
}
```

Exemple per num_new_models=1 (SINGLE FORMAT — use when num_new_models=1):
```json
{
  "model_definition_full": {
    "model_id": "llm_generated_placeholder",
    "version": "1.0.0",
    "description": "...",
    "change_log": ["..."],
    "llm_reasoning": "...",
    "potential_next_variations": ["...", "..."],
    "seed": 42,
    "architecture_definition": {},
    "training_config": {
      "batch_size": 1024,
      "epochs": 500,
      "initial_learning_rate": 0.001,
      "early_stopping_patience": 10
    }
  },
  "model_definition_summary": {
    "kind": "...",
    "focus": "...",
    "inputs": 0,
    "outputs": 0,
    "complexity": "low|medium|high",
    "expected_behavior": "...",
    "change_strategy": "...",
    "what_changed": "...",
    "experiment_goal": "...",
    "next_variations": ["...", "..."],
    "competitiveness_hypothesis": "...",
    "novelty_budget": {
      "structural_change": "...",
      "refinement_change": "..."
    }
  }
}
```
