Mode: REPAIR

Objectiu: corregir l'error amb el canvi mínim possible.
No reinventis l'arquitectura. No optimitzis si no cal.

### Context mínim
buggy_model_json:
{{buggy_model_json}}

validation_error:
{{validation_error}}

error_traceback:
{{error_traceback}}

working_model_example_json (opcional):
{{working_model_example_json}}

available_inputs_description:
{{available_inputs_description}}

available_outputs_description:
{{available_outputs_description}}

available_inputs_contract_json (EXACTE; usa aquests noms i shapes):
{{available_inputs_contract_json}}

available_outputs_contract_json (EXACTE; usa aquests targets, output_layer_name i units):
{{available_outputs_contract_json}}

### Regles estrictes de repair
1. Repara només la causa de l'error actual.
2. Mantén `used_inputs` (noms i shapes) tret que l'error sigui exactament allà.
3. No introdueixis inputs nous ni eliminis inputs existents.
4. Mantén el nombre d'outputs i `maps_to_target_config_name`.
5. No substitueixis tot el model per una variant genèrica Dense-only.
6. Verifica que els shapes d'entrada siguin compatibles amb les dades reals.
6b. Si hi ha dubte entre noms antics i nous, usa EXCLUSIVAMENT els noms d'`available_inputs_contract_json` i `available_outputs_contract_json`.
7. Si cal simplificar, fes-ho mínimament mantenint l'estructura global.
8. No facis servir claus legacy (`inputs`, `input_layers`, `shared_backbone`) dins `architecture_definition`.

### Capes suportades i paràmetres mínims
El model pot usar les següents capes suportades pel runtime de la V3. Cada capa ha de respectar els paràmetres mínims indicats.
- `Dense`: requereix `units`.
- `Conv1D`: requereix `kernel_size` i `filters`.
- `SeparableConv1D`: requereix `kernel_size`, `filters`, i `depth_multiplier` si s'especifica.
- `LSTM` / `GRU`: requereixen `units`.
- `Bidirectional`: requereix `wrapped_layer` o `inner_layer` amb `LSTM` o `GRU`.
- `TimeDistributed`: requereix `wrapped_layer` o `inner_layer`.
- `AttentionKeras` / `MultiHeadAttentionKeras`: requereixen tensors de rank >=3.
- `Add` / `Multiply` / `Concatenate`: requereixen `input_source_feature_maps` amb >=2 tensors.
- `MaxPooling1D` / `AveragePooling1D` / `GlobalMaxPooling1D` / `GlobalAveragePooling1D` / `Flatten`.
- `RepeatVector`, `GaussianNoise`, `Masking`, `Activation`, `Softmax`, `ReLU`, `LeakyReLU`, `PReLU`, `ELU`.
- `BatchNormalization`, `LayerNormalization`, `Reshape`, `LambdaSlice`.

### Errors comuns i solucions
- Si l'error és de dimensió, comprova si `Conv1D`, `LSTM`, `GRU`, `AttentionKeras` o `MultiHeadAttentionKeras` requereixen tensors de rank >=3.
- Si el model usa `TimeDistributed`, comprova que el `wrapped_layer` o `inner_layer` sigui una capa suportada i vàlida.
- Si el model usa `Bidirectional`, comprova que el `wrapped_layer` o `inner_layer` sigui `LSTM` o `GRU`.
- Si el model usa `Add`, `Multiply` o `Concatenate`, comprova que `input_source_feature_maps` sigui una llista amb almenys 2 tensors vàlids.
- Si el model falla per incompatibilitat d'inputs, mantén `used_inputs` amb els noms i shapes exactes de `available_inputs_contract_json`.
- Si el model falla per `output_heads`, mantén `maps_to_target_config_name` i `units` segons `available_outputs_contract_json`.
- Si hi ha dubte entre noms antics i nous, usa exclusivament els noms d'`available_inputs_contract_json` i `available_outputs_contract_json`.

### Guardrails d'arquitectura
- `architecture_definition` obligatori.
- `used_inputs` obligatori i no buit.
- `branches` obligatori i no buit.
- `output_heads` obligatori i no buit.
- `Dense` requereix `units`; `Conv1D` requereix `kernel_size`; `GRU/LSTM` requereixen `units`.
- `Add`/`Multiply`/`Concatenate` requereixen `input_source_feature_maps` >=2.
- Si existeix `merges`, cada merge ha de tenir `source_feature_maps` (llista no buida).

### Mini schema obligatori (V3)
`model_definition_full.architecture_definition` ha de tenir com a mínim:
```json
{
  "used_inputs": [
    {
      "input_layer_name": "...",
      "source_feature_name": "...",
      "shape": [800]
    }
  ],
  "branches": [
    {
      "name": "...",
      "input_source_layer": "...",
      "layers": [
        {"type": "Dense", "units": 64, "activation": "relu", "name": "..."}
      ],
      "output_feature_map_name": "..."
    }
  ],
  "output_heads": [
    {
      "output_layer_name": "...",
      "maps_to_target_config_name": "...",
      "source_feature_map": "...",
      "units": 1
    }
  ]
}
```

### Auto-check abans de retornar
Verifica internament:
- JSON vàlid
- camps obligatoris presents
- inputs/outputs consistents
- capes suportades
- que el JSON parseja correctament en un parser JSON estàndard
- que no hi ha claus legacy prohibides

### Sortida (OBLIGATÒRIA)
Return ONLY one valid JSON object (single object, not an array/list). Do not include any extra text.
Before returning, validate that the response is valid JSON and parseable.

Resposta amb exactament aquestes claus de primer nivell:
- `model_definition_full`
- `model_definition_summary`

Si no es pot reparar de forma raonable:
{
  "model_definition_full": {},
  "model_definition_summary": {
    "kind": "repair_failed",
    "focus": "error_unresolved",
    "inputs": 0,
    "outputs": 0,
    "complexity": "unknown",
    "expected_behavior": "manual_intervention_required"
  }
}
